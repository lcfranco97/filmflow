/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'image.tmdb.org',
            'img.freepik.com',
        ]
    }
};

export default nextConfig;
